{
  "notes": "Recommended: keep AMP on env.dt scale, delay style reward until locomotion has started to stabilize, then ramp over 2000 iterations and retain top task checkpoints.",
  "train": {
    "runner": {
      "run_name": "sw1_dt_warmup",
      "save_top_task_checkpoints": 3
    },
    "amp": {
      "style_reward_weight": 1.0,
      "scale_style_reward_by_dt": true,
      "style_reward_start_after": 1000,
      "style_reward_warmup_iterations": 2000,
      "style_reward_min_task_reward": null,
      "style_reward_max_task_ratio": 0.25
    }
  }
}
